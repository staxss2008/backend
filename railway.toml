# Railway配置文件

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "java -jar target/vehicle-management-1.0.0.war"
healthcheckPath = "/vehicle-management/api/auth/current"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
